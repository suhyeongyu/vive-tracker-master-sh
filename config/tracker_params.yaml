vive_tracker_node2:
  ros__parameters:
    publish_rate: 100.0
    battery_refresh_interval: 1.0
    rescan_interval: 2.0
    frame_id: "openvr"
    num_trackers: 2
    
vive_tracker_node:
  ros__parameters:
    publish_rate: 100.0
    frame_id: "openvr"
    num_trackers: 2

mock_tracker_node:
  ros__parameters:
    publish_rate: 100.0
    num_trackers: 2
    pattern: "circular"
    radius: 0.5
    frame_id: "openvr"

tracker_visualizer_node:
  ros__parameters:
    num_trackers: 2
    viser_port: 8080
